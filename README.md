# Python-demos
 Statistics and mathematical models in python to use for complex projects at some point
Currently building python projects for data visualisation. I aim to update this repo with new projects every 2-3 days. 

## Completed projects:
### central_limit : 
Generates k number of random samples and observes as k gets large, results are distributed normally.

### matrices :
Matrix multiplication (2x2), (3x3), (4x4), template is used for solving systems of linear equations.

### numeric_methods :
Numerical integration,takes a given function of x, and integrates over a user given interval using a riemann sum (rectangles), Simpson's rule (quadratics), and the quad() function from a python module, I have also added functions to approximate over singularities by taking f(x) over the defined interval and placing them into percentiles, and "skipping" any divergent points, such as $\` f(x) = \frac{1}{x-1}   \`$ when x=1.

### ode_solver : 
solves 1<sup>st</sup> order differential equations and plots a graph of solutions with initial conditions .

### unknown_dist
From 5 distributions (Normal, Binomial, Poisson, Geometric and Negative Binomial), calculates PDF and/or CDF for given user inputs and presents results in a clear formatting.

## Future projects
ODE solver, to solve 2<sup>nd</sup> order Differential equations using a module, and add a visual model of a solution to the ODE. At some point, I will update for coupled differential eqautions and (maybe) partial differential equations.

### confidence
Hypothesis testing different conditions in distributions, need to first learn hypothesis testing, chi² tests and sampling distributions
 
 
 
 ## AI usage:
I sometimes use AI for writing code, but I only add it to this repository if the code makes sense and not if most of the file is AI-generated.
